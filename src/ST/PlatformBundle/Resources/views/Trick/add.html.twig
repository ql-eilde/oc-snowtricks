{% extends "STPlatformBundle::layout.html.twig" %}

{% block title %}Créer un trick - {{ parent() }}{% endblock %}

{% block stplatform_body %}
    <div class="panel panel-default">
        <div class="panel-heading">
            <h1 class="panel-title">Créer un nouveau trick</h1>
        </div>
        <div class="panel-body">
            {{ include("STPlatformBundle:Trick:form.html.twig") }}
        </div>
    </div>

    <p>
        <a href="{{ path('st_platform_homepage') }}" class="btn btn-default">
            <i class="glyphicon glyphicon-chevron-left"></i>
            Retour à l'accueil
        </a>
    </p>
{% endblock %}